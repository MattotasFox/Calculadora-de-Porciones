import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calculadoradeporciones.app',
  appName: 'Calculadora de Porciones',
  webDir: 'dist'
};

export default config;
